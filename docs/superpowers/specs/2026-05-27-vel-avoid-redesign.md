# vel_avoid 引导式避障奖励重新设计

## 问题

当前 `_compute_v_avoid` 将 36 个扇区的避障速度直接向量求和。在走廊场景中，左右两侧墙壁同时激活多个扇区，左右推力叠加导致 `v_avoid` 幅值过激。一旦机器人被推向一侧，另一侧墙壁立即产生反向推力，形成振荡，机器人最终不知道往哪里走。

## 目标

将避障逻辑从"推开"改为"引导"：
- 保留向量求和的平滑方向过渡
- 用指令方向投影区分威胁优先级（正前方 > 侧面）
- 用加权平均替代加权求和，使 |v_avoid| ≤ ||v_cmd||，无论多少扇区激活都不会超过指令速度
- 每个扇区权重 `cos_i+c ∈ [c, 1+c]`，方向自然平滑，无需软截断

## 最终公式

```
每扇区 i = 0..35:
  d_i     = 扇区 i 内最小障碍距离
  w_i     = exp(-alpha × d_i) × (d_i < d_max)

v_avoid = ||v_cmd|| × Σ w_i × (-u_i) / (Σ w_i + ε)
```

36 个扇区均等对待，纯距离加权。走廊中前后扇区 away_dir 的 x 分量自然抵消，
合方向只做侧向引导。

### 参数

| 参数 | 值 | 说明 |
|------|-----|------|
| alpha | 1.0 | 距离衰减系数，复用现有 avoid_alpha |
| d_max | 1.5 | 截断距离(m)，复用现有 avoid_distance_thresh |
| n_sectors | 36 | 不变 |
| avoid_beta | 1.0 | 不变 |
| avoid_c | 0.0 | 已废弃，保留字段 |

### 为什么这套设计不自相矛盾

- 纯距离加权：前后扇区对称对待，x 分量自然抵消（走廊不转圈）
- 加权平均：|v_avoid| ≤ ||v_cmd||（不爆炸）
- 硬截断 d_max：只在实际靠近障碍物时激活（不干扰空旷前进）
- 信号简单：不做路径规划，只提供局部"别靠太近"的引导

### 平滑性分析

- **方向**：加权求和天然平滑，威胁从扇区 A 逐渐转移到扇区 B 时合方向连续旋转
- **幅值**：`w_i` 上限为 `1+c`，近处不会像纯指数 `exp(-d)` 那样爆炸增长
- **cos_i 截断**：单个扇区在 90° 处从正变 0，但 36 扇区 10° 粒度平滑了该效应

### 关键场景行为

| 场景 | d_i | 效果 |
|------|-----|------|
| 走廊中央前进 | 两侧均大 | 所有 w_i≈0，不干预 |
| 贴左墙前进 | 左小右大 | 左侧权重＞右侧，指向右方偏离 |
| 前方柱子 | 前方小 | 前方扇区权重主导，向后推开 |
| 对称走廊 | 两侧等距 | x/y 分量对称抵消，v_avoid≈0 |
| 凹形弯道 | 前方两侧小 | 加权平均指向开阔方向 |

## 修改文件

### go2_lidar_pd_risknet_config.py
- 新增 `avoid_c = 0.15`
- `avoid_distance_thresh` 语义从"激活阈值"变为"最大关注距离"

### go2_lidar_pd_risknet.py
- 重写 `_compute_v_avoid()` 方法
- `_reward_vel_avoid()` 不变
- `_draw_debug_vis()` 不变

## 参数调优指南

- `c`：机器人贴墙时的推开幅度。太小 → 擦墙甚至卡墙；太大 → 回归振荡
- `d_max`：远距离截断。减小 → 更早响应近处障碍；增大 → 更远预判。可尝试 2.0~10.0
- `alpha`：距离敏感度。增大 → 更陡峭的近处响应；减小 → 更平缓
