# vel_avoid 引导式避障奖励重新设计

## 问题

当前 `_compute_v_avoid` 将 36 个扇区的避障速度直接向量求和。在走廊场景中，左右两侧墙壁同时激活多个扇区，左右推力叠加导致 `v_avoid` 幅值过激。一旦机器人被推向一侧，另一侧墙壁立即产生反向推力，形成振荡，机器人最终不知道往哪里走。

## 目标

将避障逻辑从"推开"改为"引导"：
- 保留向量求和的平滑方向过渡
- 用指令方向投影区分威胁优先级（正前方 > 侧面）
- 每个扇区分量天然有界（`cos_i+c` ∈ [c, 1+c]），凹形包围中间相反方向的扇区自然抵消，无需软截断
- 贴近障碍时提供轻柔兜底推力，而非剧烈弹开

## 最终公式

```
v_cmd_dir = v_cmd / ||v_cmd||                     (若 ||v_cmd|| == 0, v_avoid = 0)

每扇区 i = 0..35:
  u_i     = [cos(θ_i), sin(θ_i)]                  扇区中心方向
  cos_i   = max(0, v_cmd_dir · u_i)               指令在扇区方向的投影, [0, 1]
  d_i     = 扇区 i 内最小障碍距离
  w_i     = (cos_i + c) × exp(-alpha × d_i) × (d_i < d_max)

v_avoid = ||v_cmd|| × Σ w_i × (-u_i)
```

### 参数

| 参数 | 值 | 说明 |
|------|-----|------|
| c | 0.15 | 侧面兜底推力比例，占指令速度的 15% |
| alpha | 1.0 | 距离衰减系数，复用现有 avoid_alpha |
| d_max | 1.5 | 截断距离(m)，复用现有 avoid_distance_thresh |
| n_sectors | 36 | 不变 |
| avoid_beta | 1.0 | 不变 |

### 为什么不需要软截断

- `w_i` 天然有界：`(cos_i + c)` ∈ [c, 1+c]，乘以 exp(-αd) ∈ (0,1]，每个扇区贡献上限为 1+c
- `cos_i` 的 max(0) 截断只保留前方半球，后方墙壁不参与
- 凹形弯道中前方两侧墙壁的向量方向相反（左下+右下），求和时自然削弱而非叠加
- 唯一可能叠加的场景是同侧长墙的多个相邻扇区，但语义上墙越宽威胁越大，叠加是合理的

### 平滑性分析

- **方向**：加权求和天然平滑，威胁从扇区 A 逐渐转移到扇区 B 时合方向连续旋转
- **幅值**：`w_i` 上限为 `1+c`，近处不会像纯指数 `exp(-d)` 那样爆炸增长
- **cos_i 截断**：单个扇区在 90° 处从正变 0，但 36 扇区 10° 粒度平滑了该效应

### 关键场景行为

| 场景 | cos_i | d_i | w_i | 效果 |
|------|-------|-----|-----|------|
| 走廊中央前进 | 侧墙≈0 | 大 | ≈0 | 不干预 |
| 贴左墙前进 | 侧墙≈0 | 小 | ≈c | 轻柔右推，15% |
| 指令指向墙壁 | ≈1 | 中 | (1+c)×exp(-αd) | 强修正 |
| 正前方柱子 | ≈1 | 很小 | ≈1+c | 最强推开 |
| 凹形弯道 | 两侧墙≈0.5 | 小 | 方向相反互相抵消 | 自然平衡 |

## 修改文件

### go2_lidar_pd_risknet_config.py
- 新增 `avoid_c = 0.15`
- `avoid_distance_thresh` 语义从"激活阈值"变为"最大关注距离"

### go2_lidar_pd_risknet.py
- 重写 `_compute_v_avoid()` 方法
- `_reward_vel_avoid()` 不变
- `_draw_debug_vis()` 不变

## 参数调优指南

- `c`：机器人贴墙时的推开幅度。太小 → 擦墙甚至卡墙；太大 → 回归振荡
- `d_max`：远距离截断。减小 → 更早响应近处障碍；增大 → 更远预判。可尝试 2.0~10.0
- `alpha`：距离敏感度。增大 → 更陡峭的近处响应；减小 → 更平缓
