# 指令优先 + 扇区约束速度：奖励重构设计

## 动机

现有 Omni-Perception 奖励设计以"自主避障 + 探索开阔空间"为目标：
- `vel_avoid`：将 v_avoid（远离障碍物的排斥向量）叠加到速度目标上，把机器人"推开"
- `rays`：计算开阔空间方向，奖励转向该方向的角速度，把机器人"拉向"空旷处

新任务目标：**指令优先，避障次之**。机器人应严格执行命令，在被命令的方向上贴着障碍物走、在指定位置停下，而非主动寻找开阔空间。

## 核心设计理念

**障碍物不应决定机器人"往哪走"，只应约束"在该方向上能走多快"。**

```
旧: cmd → 加入 v_avoid 偏移 → 实际速度目标
新: cmd → 每个方向分量 × 该方向安全因子 → 安全速度目标
```

## 新建 / 变更内容

### 新建文件

| 文件 | 说明 |
|------|------|
| `go2_cmd_safe_config.py` | 新训练配置，传感器中心放置 + 新奖励 |
| `go2_cmd_safe.py` | 新环境类，继承 Go2LidarPDRiskNet |
| 新地形配置 | 适配指令优先场景（含随机高度障碍物） |

### 保留不变

| 内容 | 说明 |
|------|------|
| PD-RiskNet 网络 | 架构、参数、特权监督全保留 |
| 碰撞回退 | 头部/base 碰撞终止 + 腿部碰撞概率回退 |
| 卡住检测 | 双条件（v_low/d_low）+ 3s 计时，无终止惩罚 |
| 现有 config | `go2_lidar_pd_risknet_config.py` 不动 |
| 辅助奖励 | torques, dof_vel, dof_acc, action_rate, feet_stumble 等 |
| `channel_forward` | 沿通道前进奖励保留 |
| `goal` / `termination` / `collision` | 保留 |

### 删除

| 删除 | 原因 |
|------|------|
| `vel_avoid` | 不再用排斥向量偏移速度目标 |
| `rays` | 不再引导转向开阔空间 |

### 传感器位姿变更

```python
offset_pos = [0.0, 0.0, 0.0]  # LiDAR 置于身体中心，水平放置
```

理由：仿真验证环境，LiDAR 置于身体中心使所有方向的射线距离直接等于身体中心到障碍物的距离，无需椭圆补偿。且水平放置更能体现避障需求。

## 新奖励结构

| 奖励项 | 权重 | 类型 | 说明 |
|--------|:---:|------|------|
| `cmd_safe_vel` | 2.0 | 核心 | 扇区约束的 2D 命令速度跟踪 |
| `sector_dist_penalty` | 0.5 | 背景 | 全向扇区距离惩罚 |
| `channel_forward` | 10.0 | 辅助 | 沿通道前进 |
| `collision` | -0.02 | 安全 | 连杆碰撞力 |
| `termination` | -10.0 | 安全 | 终止惩罚 |
| `goal` | 20.0 | 任务 | 终点到达 |

## 点云预处理：z 坐标过滤

在计算扇区距离之前，先过滤掉身体帧中 z 坐标过高的点：

```
对每条射线 i (在 avoid_distances 计算中):
    如果 lidar_points_base[i, 2] > z_thresh:
        忽略此点 (设置为 max_distance，不参与扇区 min())

z_thresh = 0.20 (身体帧，相对 LiDAR 中心)
```

### 机制：通过降低身体高度使浮空障碍物"消失"

LiDAR 在身体中心 [0,0,0]。机器人站立时身体高度约 0.34m：

```
                           ┌─ 横杆悬在 0.5m ─┐

身体帧 z 坐标 (LiDAR 中心 = 0):

  站立 (body_height≈0.34):
    横杆点云 z ≈ 0.5 - 0.34 = 0.16  → z < 0.20, 被计入 → 惩罚!减速!

  蹲下 (body_height≈0.20):
    横杆点云 z ≈ 0.5 - 0.20 = 0.30  → z > 0.20, 被过滤 → 无惩罚!正常前进!
```

**策略学到的行为链：** 看到头顶有障碍物 → 降低身体 → 横杆从奖励计算中"消失" → `cmd_safe_vel` 不再被压制 → 正常前进获得奖励。

**关键：不会误过滤地面障碍物。** 从地面延伸到高处的墙壁总有点云落在 z < 0.20 的范围内（墙底部），因此不会被过滤。只有**纯粹的浮空障碍物**（所有射线击中点都高于 z_thresh）才会被完全排除。

这是一个**被动引导机制**——不需要额外的"蹲下奖励"项，策略通过改变自身姿态自然获得更高的前进奖励。

## 核心公式：cmd_safe_vel

### Step 1: 逐扇区身体半径

Go2 身体近似为椭圆：半长 a = 0.35m，半宽 b = 0.15m。LiDAR 在 (0, 0)。

对每个扇区 j（方向角 θ_j）：

```
解方程: (r*cos(θ))²/a² + (r*sin(θ))²/b² = 1
→ body_radius_j = a*b / sqrt(b²*cos²(θ) + a²*sin²(θ))
```

36 个常数预计算，不参与运行时计算。有效距离 `d_eff_j = d_j - body_radius_j` 表示从身体表面到障碍物的实际间隙。

### Step 2: 扇区安全因子

```
d_eff_j = max(0, d_j - body_radius_j)                   -- 从身体表面起算的有效距离
safe_j  = clamp((d_eff_j - d_safety) / (d_safe_max - d_safety), 0, 1)

d_safety   = 0.10   -- 额外安全间隙
d_safe_max = 1.0    -- 超过此距离视为完全安全
```

### Step 3: 命令方向 2D 安全速度

```
cmd_2d = commands[:, :2]   # 基座帧
cmd_norm = ||cmd_2d||

对每个扇区 j:
    align_j = max(0, dot(cmd_2d, sector_center_j))   # cmd 在该方向的分量
    v_safe_2d += safe_j * align_j * sector_center_j   # 安全因子缩放

v_safe_2d = clamp_norm(v_safe_2d, max=cmd_norm)      # 不超过命令速度
```

### Step 4: 奖励

```
v_actual = base_lin_vel[:, :2]
r_cmd_safe_vel = exp(-||v_actual - v_safe_2d||² / σ²)

σ = 0.25
```

### 行为推演

| 场景 | v_safe_2d 行为 | 策略表现 |
|------|---------------|----------|
| 开阔地 + cmd=1.0 向前 | safe≈1 对所有前方扇区，v_safe≈cmd | 全速跟指令 |
| 窄通道 + cmd=1.0 向前 | 侧面扇区 safe≈0.4，v_safe 整体略降 | 减速通过 |
| 右墙 + cmd=[0.8, 0.3] | 前进分量保留，右向分量被抑制 | 沿墙滑行，不撞墙 |
| 前方 0.5m 墙 + cmd=1.0 向前 | 前方扇区 safe≈0，v_safe≈0 | 减速到几乎停止 |
| cmd=0（停车） | v_safe=0，任何移动都远离目标 | 停住 |

## 辅助公式：sector_dist_penalty

对所有 36 个扇区生效的全向背景惩罚：

```
r_dist = -α * mean_j(relu(d_thresh - d_eff_j)²)

d_thresh = 0.5m
α = 0.5
```

提供早期的"接近障碍物"预警信号。全向生效（包括后方），防止任何方向的贴墙依赖。

## 浮空障碍物：z 过滤被动引导

见上方"点云预处理"节。通过过滤 z > 0.20 的点云，策略可以通过降低身体高度使浮空障碍物从奖励计算中"消失"，从而自然获得前进奖励。这是一个被动引导机制，无需额外奖励项。

实验观察点：策略是否能学会"看到悬杆 → 压低身体 → 钻过去"的行为链。

---

## 实验计划

1. 创建新 config + 环境类 + 任务注册
2. 配置地形：通道 + 随机高度障碍物块
3. 用固定命令（纯前进，无转弯）做 smoke test，验证奖励计算正确
4. 小规模训练（512 envs, 200 iters），观察：
   - `cmd_safe_vel` 和 `sector_dist_penalty` 的 episode sum 曲线
   - 机器人是否表现出"靠墙减速、开阔加速"的行为
5. 观察不同障碍物高度下机器人的通行行为
