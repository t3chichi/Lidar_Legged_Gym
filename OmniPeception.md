# Omni-Perception：动态环境中足式机器人移动的全向碰撞避免


https://github.com/aCodeDog/OmniPerception

<center>图 1：Omni-Perception 框架的验证场景。左侧展示了有效的全向碰撞避免，机器人对来自不同接近方向的障碍物做出反应。右侧展示了对多样化环境特征的鲁棒性，包括成功穿越空中、透明、细长和地面障碍物。这些结果突显了 Omni-Perception 直接从原始 LiDAR 输入实现复杂 3D 环境中无碰撞运动的能力。</center>

**摘要：** 在复杂的 3D 环境中实现敏捷运动需要强大的空间感知能力，以安全避开各类障碍物，例如空中杂物、不平坦地形和动态智能体。基于深度的感知方法通常面临传感器噪声、光照变化以及中间表示带来的计算开销等问题。相比之下，将 LiDAR 感知直接集成到足式机器人端到端学习中的研究仍处于探索不足的状态。我们提出了 Omni-Perception，一种端到端的运动策略，通过直接处理原始 LiDAR 点云实现 3D 空间感知和全向碰撞避免。其核心是 PD-RiskNet（近端-远端风险感知层次网络），这是一个新颖的感知模块，用于解析时空 LiDAR 数据以进行环境风险评估。为了促进高效的策略学习，我们开发了一个高保真 LiDAR 仿真工具包，具备真实噪声建模和快速光线投射功能，兼容 Isaac Gym、Genesis 和 MuJoCo 等平台，从而实现可扩展的训练和有效的 Sim-to-Real 迁移。直接从原始 LiDAR 数据学习反应式控制策略，使机器人能够比依赖中间地图或有限感知的方法更鲁棒地导航包含静态和动态障碍物的复杂环境。我们通过真实世界实验和大规模仿真验证了 Omni-Perception，展示了其在高度动态环境中强大的全向避障能力和卓越的运动性能。

**关键词：** 足式机器人，运动，强化学习，LiDAR 感知

---

## 1 引言

足式机器人具有独特的潜力，能够在轮式或履带式系统经常失效的、以人为中心的复杂环境中导航。然而，要充分发挥其能力，不仅需要敏捷的运动，还需要鲁棒的感知和对多样化 3D 周围环境的快速反应 [1, 2]。在静态和动态障碍物（从地面杂物和不平坦地形到悬垂结构和移动智能体）中实现安全、高速的运动，需要整体的空间感知和紧密耦合的反应式控制 [3, 4]。在非结构化 3D 环境中实现这种敏捷、无碰撞的运动仍然是一个根本性挑战 [5, 6]。

大多数最先进的控制器仅使用本体感觉进行盲操作，限制了它们处理复杂地形或突发障碍物的能力 [7, 8, 9, 2, 10, 11, 12]。虽然结合了深度相机外部感知的方法 [13, 14] 在不平坦地形上的运动方面取得了进展，但基于深度的感知存在局限性，例如对光照条件敏感、视场有限、易受噪声影响以及维护高程图等中间表示的计算开销 [15, 16]。这些中间表示反过来又难以处理空中杂物和复杂的非平面障碍物。将导航规划与运动控制解耦的替代方法 [17, 18] 通常导致保守行为，阻碍了机器人敏捷性的充分发挥。

与此同时，LiDAR 传感器已成为自动驾驶等领域的核心工具，提供密集、不受光照影响的 3D 测量，并用于操作任务 [19, 20, 21, 22, 23, 24, 25, 26]。LiDAR 提供了丰富的几何信息，非常适合导航空间复杂的环境。尽管前景广阔，但将 LiDAR——尤其是直接点云处理——集成到足式机器人端到端学习中的研究仍然很少 [27, 28, 29]。这一差距源于在快速控制回路中实时处理点云的挑战，以及为有效 Sim-to-Real 迁移精确建模 LiDAR 物理特性的困难 [30]。

本工作旨在弥合这一关键差距。我们假定，在端到端强化学习策略中直接利用原始、时空 LiDAR 点云，可以为足式机器人解锁鲁棒的 3D 环境感知能力。这使机器人能够实现全向碰撞避免和敏捷导航，而无需依赖手工制作的中间表示或受限的解耦规划。我们引入了 Omni-Perception，这是一个以在高保真 LiDAR 模拟器中通过 RL 训练的端到端策略为中心的新颖框架。我们的核心技术贡献是近端-远端风险感知层次网络，一种新颖的感知架构，能够高效处理原始时空 LiDAR 数据流，以评估多级环境风险。为了实现可扩展和现实的训练，我们还开发了一个高保真、跨平台的 LiDAR 仿真工具包，具备真实噪声建模和高效并行光线投射功能，支持包括 Isaac Gym、Genesis、MuJoCo 等在内的多种物理引擎。通过直接从 3D 空间数据学习，Omni-Perception 使足式机器人能够在动态跟踪速度指令的同时，避开复杂的多轴威胁（例如空中障碍物、地面陷阱、移动智能体），推动了非结构化环境中敏捷和安全导航的边界。

我们的贡献总结如下：

1. **端到端 LiDAR 驱动的运动框架**：我们提出了 Omni-Perception，这是第一个通过端到端 RL 架构直接处理原始 LiDAR 点云，为足式机器人实现 3D 空间感知的框架。
2. **新颖的 LiDAR 感知网络**：PD-RiskNet。
3. **高保真 LiDAR 仿真工具包**：我们开发了一个高性能、跨平台的 LiDAR 仿真工具包，具备真实噪声模型和快速并行光线投射，实现了有效的零样本 Sim-to-Real 策略迁移。
4. **鲁棒 LiDAR 驱动敏捷性的验证**：我们通过广泛的仿真和真实世界实验验证了 Omni-Perception，展示了在包含静态和动态 3D 障碍物的多样且具有挑战性的环境中，强大的速度跟踪和全向避障能力。

---

## 2 相关工作

### 2.1 基于学习的足式机器人运动

强化学习已成为开发足式机器人复杂运动控制器的强大范式，在许多场景中展现出超越传统基于模型方法的显著敏捷性和鲁棒性 [9, 31, 32]。RL 方法已成功生成了用于高速奔跑 [8, 11] 和挑战性地形穿越 [33, 7] 的控制器。这些策略通常将本体感觉状态——在某些情况下，还有有限的外部感知信息——直接映射到关节指令。

### 2.2 用于运动的外部感知

整合环境感知对于使足式机器人能够在未知或动态环境中移动至关重要。

**基于深度的感知：** 大量工作利用深度相机。许多方法将环境重建为中间表示，例如 2.5D 高程图，用于落足点规划或策略输入 [34, 35]。然而，高程图本身难以处理非平面障碍物，如悬垂物或空中杂物，并且对传感器噪声敏感。其他工作尝试更直接地使用深度数据，要么从深度图像学习端到端策略 [36, 37, 13, 38]，要么从深度中提取稀疏特征，如射线距离 [3]。然而，这些方法继承了深度传感器的局限性，包括对光照敏感和范围有限。此外，Sim-to-Real 迁移通常需要广泛的域随机化，特别是向深度数据注入噪声，以解决真实世界的传感器特性 [37]。

**基于 LiDAR 的感知：** LiDAR 传感器提供直接的 3D 测量，并且基本上不受光照条件影响，使其在自动驾驶和移动机器人建图等领域成为基础传感器 [39, 40, 41]。然而，它们在基于学习的、端到端的足式机器人运动中的应用非常有限。现有的使用通常涉及传统流程（SLAM、路径规划），而不是直接集成到 RL 策略中 [30, 27, 42]。挑战包括点云的高维度和非结构化特性、在高控制频率下处理点云的计算成本，以及为训练创建精确、高效的 LiDAR 仿真的难度 [43, 44]。尽管一些工作使用模拟的类 LiDAR 输入（例如，射线投射）进行感知 [3]，但据我们所知，Omni-Perception 是第一个直接从原始时空 LiDAR 点云学习端到端运动策略的框架，实现了鲁棒的全向碰撞避免和导航。

### 2.3 移动机器人的碰撞避免

碰撞避免是机器人学中的一个基本问题 [45]。经典方法通常依赖几何方法和构型空间中的显式规划 [46, 47, 48]。模型预测控制已被应用于足式机器人，将碰撞约束纳入优化中 [49, 50]。然而，MPC 依赖精确的动力学模型，计算量大，并且为了确保约束满足往往导致运动缓慢 [51, 52]。

基于学习的方法，特别是 RL，通过使机器人能够学习反应式避障行为提供了一种替代方案 [3, 53]。在空中机器人学中，RL 已成功应用于使用 RGB-D 相机或模拟激光扫描作为感知输入的动态导航任务 [54]。我们的工作推进了这一方向，引入了一种端到端的基于 RL 的碰撞避免策略，该策略利用来自原始 LiDAR 数据流的丰富 3D 信息，并由我们专门的 PD-RiskNet 处理，紧密集成在运动控制策略中。

---

## 3 方法

### 3.1 问题表述

我们致力于解决为足式机器人学习连续运动策略 \(\pi\) 的问题，使其能够在跟踪期望速度指令的同时，利用机载感知在复杂 3D 环境中执行全向避障。该策略在具有真实 LiDAR 感知的高保真仿真环境中通过端到端 RL 进行训练。

核心任务是学习一个策略 \(\pi :\mathcal{O}\to \Delta (\mathcal{A})\)，在每个时间步 \(t\) 将观测 \(o_{t}\in \mathcal{O}\) 映射到动作 \(a_{t}\in \mathcal{A}\) 上的分布。观测空间 \(\mathcal{O}\) 包含策略在时间 \(t\) 可用的信息。它包括：

- **本体感觉状态** \(o_{t}^{\mathrm{prop}}\)：一段长度为 \(N_{hist}\) 的运动学信息历史，包括关节位置 \(q_{t}\)、关节速度 \(\dot{q}_{t}\)、基座线速度和角速度 \(v_{t},\omega_{t}\)，以及由投影重力表示的基座姿态 \(g_{t}\)。
- **外部感知状态** \(P_{t}\)：一段长度为 \(N_{hist}\) 的原始 3D 点云数据历史，提供关于周围环境的时空信息。
- **任务指令** \(c_{t}\)：期望的速度指令，通常是机器人基座坐标系下的目标线速度 \(v_{t}^{\mathrm{cmd}}\) 和角速度 \(\omega_{t}^{\mathrm{cmd}}\)。

因此，观测为 \(o_{t} = (o_{t - N_{hist} + 1:t}^{\mathrm{prop}},P_{t - N_{hist} + 1:t},c_{t})\)。我们提出的感知网络 PD-RiskNet（第 3.3.1 节）处理点云序列 \(P_{t - N_{hist} + 1:t}\)，并将提取的特征与本体感觉历史 \(o_{t - N_{hist} + 1:t}^{\mathrm{prop}}\) 和指令 \(c_{t}\) 集成，以指导策略。

动作空间 \(\mathcal{A}\) 由低级电机指令 \(a_{t}\) 组成，具体是下发给机器人执行器的目标关节位置。我们将学习问题构建为在无限时域折扣马尔可夫决策过程中优化策略，该 MDP 由元组 \((\mathcal{S},\mathcal{A},r,\gamma ,T)\) 定义。虽然策略直接使用观测 \(o_{t}\)，但底层状态 \(s_{t}\in \mathcal{S}\) 可能包含隐藏的模拟器状态或历史。转移动力学 \(T:\mathcal{S}\times \mathcal{A}\to \Delta (\mathcal{S})\) 由物理模拟器和传感器模型决定。奖励函数 \(r:\mathcal{S}\times \mathcal{A}\to \mathbb{R}\)（或近似为 \(r(o_{t},a_{t})\)）旨在鼓励特定行为（详见第 3.3.2 节）。目标是找到最优策略 \(\pi^{*}\)，最大化期望的折扣未来奖励总和：

\[
\pi^{*} = \arg \max_{\pi}\mathbb{E}_{\tau \sim \pi}\left[\sum_{t = 0}^{\infty}\gamma^{t}r(s_{t},a_{t})\right], \quad (1)
\]

其中 \(\tau = (s_{0},a_{0},s_{1},a_{1},\ldots)\) 是在策略 \(\pi\) 下生成的轨迹，\(a_{t}\sim \pi (o_{t})\)，并且 \(\gamma \in [0,1)\) 是折扣因子。

### 3.2 自定义 LiDAR 渲染框架

**动机与后端。** 虽然现有的模拟器，如 NVIDIA Isaac Gym/Sim [55]、Gazebo [56]、Genesis [57]，提供了物理仿真能力，但它们的渲染解决方案通常缺乏对多样化 LiDAR 传感器模型（如非重复扫描 LiDAR）的支持，或者在并行性和跨平台执行方面存在限制。为解决这些不足，受 [58] 启发，我们引入了一个自定义的高性能 LiDAR 渲染框架，该框架利用 NVIDIA Warp [59] 进行 GPU 加速，并利用 Taichi [60] 实现跨平台兼容性，从而能够在缺少专用 NVIDIA GPU 的系统（包括仅 CPU 环境）上执行。

**用于并行仿真的优化网格管理。** 模拟大量包含频繁移动的动态对象的环境构成了重大挑战。在每个时间步为每个环境独立地变换每个网格并重建加速结构（BVH）[61] 在此类场景中将变得计算上不可行。为了克服这一瓶颈，我们采用了一种专门的网格划分和更新策略：

- **每个环境的静态网格：** 对于每个环境 \(\mathcal{E}_i\)，非移动几何体（地形、固定障碍物）被表示为一个静态网格 \(\mathcal{M}_i^{\mathrm{static}}\)。其 BVH 结构仅在初始化时构建一次，从而最小化冗余计算。
- **全局共享动态网格：** 我们将所有动态实体（例如，机器人部件、移动障碍物）的网格从所有 \(N_{\mathrm{envs}}\) 个并行环境聚合到一个单一的全局动态网格 \(\mathcal{M}_{\mathrm{global},t}^{\mathrm{dynamic}}\) 中。在仿真时间 \(t\)，仅根据相应动态对象 \(\mathcal{M}_j^{\mathrm{dynamic}}\) 的最新变换 \((\mathbf{T}_{j,t})\) 更新此共享网格的顶点位置。

**同步更新与光线投射。** LiDAR 仿真涉及针对相应的静态网格 \(\mathcal{M}_i^{\mathrm{static}}\) 和单一的全局动态网格 \(\mathcal{M}_{\mathrm{global},t}^{\mathrm{dynamic}}\) 进行光线投射。至关重要的是，\(\mathcal{M}_{\mathrm{global},t}^{\mathrm{dynamic}}\) 的顶点更新对于所有环境同步发生，通常与传感器的更新频率匹配。这种集中式更新机制避免了在每个时间步为每个 \(N_{\mathrm{envs}}\) 环境单独更新动态网格和重建其加速结构的巨大开销。这使得所有并行环境能够高效地查询相同的最新动态几何体，从而在不同硬件配置上实现可扩展、高性能的仿真。

**LiDAR 模型支持与扫描模式仿真。** 利用此框架，我们实现了对多种商用 LiDAR 模型的支持。这包括非重复扫描 LiDAR，例如 Livox 系列（如 Mid-360、Avia），我们通过使用真实设备数据确定与仿真时间 \(t\) 同步的扫描角度来模拟其特征扫描模式。此外，还支持传统的旋转式 LiDAR，预配置的模型包括 Velodyne 系列（如 HDL-64、VLP-32）和各种 Ouster 传感器。

### 3.3 Omni-Perception 框架

我们的框架，如图 2 所示，由用于处理 LiDAR 数据的 PD-RiskNet 和将此感知与本体感觉及指令集成的运动策略组成。

<center>图 2：所提出的系统框架。(a) 不同传感器覆盖范围的可视化：深度相机通常较窄、前向的视场（上）与 LiDAR 传感器更宽广、更长距离的覆盖范围（下）对比，展示在 Unitree Go2 机器人上。(b) 感知与控制管线的详细图示。原始点云历史通过 PD-RiskNet 内的两条路径处理（远端特征采用平均下采样，近端特征采用最远点采样，每个馈送至 GRU），生成空间嵌入。在点云历史可视化中，密集的蓝色点表示近端点云，稀疏的红色点表示远端点云。这些嵌入与由历史包装器处理的历史本体感觉和指令串联，并使用特权高度信息进行监督。组合的特征输入到 Actor（MLP）网络，该网络输出用于 Sim-to-Real 训练和部署的执行器目标。</center>

#### 3.3.1 PD-RiskNet：处理时空点云

PD-RiskNet 架构旨在处理从足式机器人 LiDAR 传感器获取的时空点云数据。初始步骤涉及将原始点云 \(P_{raw}\) 划分为两个不同的子集：近端点云 \(P_{proximal}\) 和远端点云 \(P_{distal}\)。此划分基于垂直角度阈值 \(\theta\)，将近场点（较高 \(\theta\)）与远场点（较低 \(\theta\)）分开，有效分离密集的局部几何体与稀疏的远处观测。

**近端点云处理：** \(P_{proximal}\) 代表近场环境，通常具有较高的点密度和相对较小的距离变化。为了高效处理此密集数据同时保留关键的局部几何细节，采用最远点采样 [62] 获取稀疏但有代表性的点子集。采样后，通过基于球坐标（先 \(\theta\) 后 \(\phi\)）排序来结构化这些点。采样后近端点云的有序表示随后输入一个专用的门控循环单元。此 GRU 的特征提取能力在训练期间通过特权高度信号进行增强监督，引导网络学习与局部地形特性和足式运动潜在风险特别相关的表示。

**远端点云处理：** 相反，\(P_{distal}\) 覆盖远场环境，通常是稀疏的，并且距离值变化较大。为处理此稀疏数据中的特性并减少离群值影响，应用平均下采样。此操作产生远处结构更均匀的表示。将当前和前 \(N_{hist}\) 帧的下采样远端点云组合，形成时空序列。该序列在下采样后通过基于球坐标（先 \(\theta\) 后 \(\phi\)）排序来结构化，并由一个单独的 GRU 模块处理，以提取捕捉远距离动态环境上下文的特征。

#### 3.3.2 风险感知运动策略

运动策略实现为一个 MLP，它接收来自 PD-RiskNet 的串联特征和本体感觉历史作为输入。

**观测空间。** 如第 3.1 节所述，观测包括本体感觉历史 \(o_{t - N_{hist} + 1:t}^{pro}\)、来自 PD-RiskNet 的处理后 LiDAR 特征 \(f_{t}^{PD}\)（源自 \(P_{t - N_{hist} + 1:t}\)）以及当前速度指令 \(c_{t}\)。因此，策略输入表示为 \(o_{t}^{policy} = (o_{t - N_{hist} + 1:t}^{pro},f_{t}^{PD},c_{t})\)。

**动作空间。** 策略输出用于低级 PD 控制器的目标关节位置 \(a_{t}\)。

**奖励函数。** 奖励函数 \(r_{t} = r(s_{t},a_{t})\) 旨在训练机器人跟踪速度指令，同时利用 LiDAR 数据主动避免碰撞。我们使用了与 [13] 类似的奖励函数。完整的奖励函数列表可在附录 5 中找到。它主要包含两个专注于速度跟踪与集成避障以及最大化环境间隙的新颖组成部分：
(1) **带避障的线速度跟踪** \(r_{vel,avoid}\)：这鼓励跟踪一个修正的目标线速度，该速度将外部指令 \(v_{t}^{cmd}\) 与动态计算的避障速度 \(V_{t}^{avoid}\) 相结合。避障速度 \(V_{t}^{avoid}\) 主动将机器人推离 LiDAR 检测到的附近障碍物。机器人的 \(360^{\circ}\) 水平周围环境被划分为 \(N_{sec} = 36\) 个角度扇区。在每个扇区 \(j\) 内，找到最小障碍物距离 \(d_{t}^{j}\)。如果 \(d_{t}^{j}\) 低于阈值 \(d_{t}^{thresh}\)（例如，\(1\mathrm{m}\)），则生成一个避障速度分量 \(V_{t,j}^{avoid}\)，指向远离扇区 \(j\) 的方向。其大小根据接近程度计算：

\[
\| V_{t,j}^{avoid}\| = \exp (-d_{j,t}\cdot \alpha_{avoid})
\]

总避障速度 \(V_{t}^{avoid} = \sum_{j = 1}^{N_{sec}}V_{t,j}^{avoid}\) 是所有扇区的向量和。该奖励项惩罚与此组合目标速度的偏差：

\[
r_{vel,avoid} = \exp (-\beta_{va}*\| v_{t} - (v_{t}^{cmd} + V_{avoid,t})\|^{2})
\]

<center>图 3：基于扇区的避障速度计算。</center>

其中 \(v_{t}\) 是基座线速度，\(\beta_{v a}\) 是权重系数。
(2) **距离最大化** \(r_{r a y s}\)：该奖励鼓励通过最大化截断的 LiDAR 射线距离来维持安全裕度并推向开阔区域。使用来自 \(n\) 条代表性远端 LiDAR 射线的截断距离 \(\hat{d}_{t,i} = \min (d_{t,i},d_{\mathrm{max}})\)，奖励为：

\[
r_{r a y s} = \sum_{i = 1}^{n}\frac{\hat{d}_{t,i}}{n\cdot d_{\mathrm{max}}}
\]

**域随机化。** 在训练期间应用此技术以增强 Sim-to-Real 迁移。这包括随机化机器人物理参数 [7] 和 LiDAR 感知特性（掩码、噪声）。详见附录 7。

---

## 4 实验

### 4.1 LiDAR 保真度评估

如图 4 所示，我们验证了 LiDAR 模拟器的保真度。我们将其输出与使用 Livox Mid-360 传感器的 Unitree G1 机器人真实数据以及使用相同水平和垂直角度的 Isaac Sim LiDAR 进行了比较。我们的仿真包括传感器的非重复扫描模式和机器人自遮挡。模拟的和真实的点分布及结构看起来相似。这一结果表明了模拟器的高保真度，有助于缩小 LiDAR 策略的 Sim2Real 感知差距。更多 LiDAR 模型扫描模式可在附录中找到。

<center>图 4：Unitree G1 机器人的仿真与真实点云对比。(a) 物理 Unitree G1 机器人设置。(b) 机载 Livox Mid-360 传感器捕获的真实世界 LiDAR 扫描。(c) （我们的）在 Isaac Gym 内使用我们的 Livox Mid-360 传感器模型生成的点云。(d) 在 Isaac Sim 内使用官方传感器生成的点云。我们的方法捕捉到了与真实 LiDAR 中一致的自遮挡效应。</center>

**表 1：不同配置下静态场景的渲染时间（毫秒）。我们的方法比 Isaac Sim 高效得多。**

<table>
<tr><td rowspan="3">射线数</td><td colspan="8">环境数量</td></tr>
<tr><td colspan="2">256</td><td colspan="2">512</td><td colspan="2">1024</td><td colspan="2">2048</td><td>4096</td></tr>
<tr><td>Ours</td><td>IsaacSim</td><td>Ours</td><td>IsaacSim</td><td>Ours</td><td>IsaacSim</td><td>Ours</td><td>IsaacSim</td><td>Ours</td></tr>
<tr><td>1k</td><td>2.5±0.3</td><td>2.1±0.7</td><td>2.1±0.4</td><td>19±1.7</td><td>2.2±0.1</td><td>32±0.8</td><td>3.4±0.2</td><td>40±1.8</td><td>-</td></tr>
<tr><td>4k</td><td>3.3±0.4</td><td>28±0.6</td><td>5.7±0.3</td><td>39±0.3</td><td>16.2±1.1</td><td>81±1.1</td><td>49.9±2.3</td><td>146±4.8</td><td>-</td></tr>
<tr><td>16k</td><td>3.7±0.1</td><td>81±3.1</td><td>11.2±0.8</td><td>142±1.4</td><td>30.4±2.8</td><td>317±17.3</td><td>118.3±4.7</td><td>600±8.8</td><td>-</td></tr>
<tr><td>32k</td><td>5.8±0.1</td><td>148±2.3</td><td>22.9±2.6</td><td>286±2.7</td><td>47.9±0.8</td><td>569±14.9</td><td>133.5±8.4</td><td>1133±32.3</td><td>-</td></tr>
</table>

### 4.2 与现有模拟器的比较和效率分析

尽管存在众多机器人模拟器，但对 LiDAR 仿真的官方支持相对有限，Gazebo 和 NVIDIA Isaac Sim 是提供内置功能的突出例子。我们根据评估和公开可获得的文档，将我们的 LiDAR 工具包功能与这些成熟平台进行比较。表 2 总结了这些平台的关键 LiDAR 仿真能力。表 1 显示了我们的 LiDAR 实现相对于 Isaac Sim 的渲染速度。我们的实现旨在结合广泛的功能支持与高性能并行执行环境，如 Isaac Gym 和 Genesis。更多细节可在附录 E 中找到。

**表 2：跨平台能力比较。**

<table>
<tr><th>LiDAR 仿真功能</th><th>Ours</th><th>Isaac Sim</th><th>Gazebo</th></tr>
<tr><td>旋转式 LiDAR 支持</td><td>✓</td><td>✓</td><td>✓</td></tr>
<tr><td>固态 LiDAR 支持</td><td>✓</td><td>✓</td><td>✓</td></tr>
<tr><td>混合固态 LiDAR (非重复扫描)</td><td>✓</td><td>×</td><td>✓</td></tr>
<tr><td>静态不规则物体</td><td>✓</td><td>✓</td><td>✓</td></tr>
<tr><td>动态不规则物体</td><td>✓</td><td>×</td><td>×</td></tr>
<tr><td>自遮挡</td><td>✓</td><td>×</td><td>×</td></tr>
<tr><td>跨平台支持</td><td>✓</td><td>×</td><td>✓</td></tr>
<tr><td>大规模并行执行</td><td>✓</td><td>✓</td><td>×</td></tr>
</table>

### 4.3 PD-RiskNet 消融研究

我们测试了四种处理 LiDAR 点云的方法，以验证我们的 PD-RiskNet 是否有效。我们比较了在 Isaac Gym 仿真中提取相同维度点云特征的四种方法，场景包含 6 个动态障碍物（速度 1.5 m/s），每种方法运行 30 次试验。如图 6 所示，任务是到达一个 \(12\mathrm{m}\times 4\mathrm{m}\) 区域的终点。表 3 显示，直接使用 MLP 的方法因内存不足（OOM）（24GB）而失败。使用 FPS 下采样、FPS+MLP 和 FPS+GRU 的方法成功率非常低，大多数失败是由于超时或卡住，而非碰撞。

**表 3：PD-RiskNet 消融结果（30 次试验）**

<table>
<tr><th>方法</th><th>成功率 (%)</th><th>碰撞率 (%)</th></tr>
<tr><td>Direct MLP</td><td>OOM*</td><td>OOM*</td></tr>
<tr><td>FPS + MLP</td><td>33.3%</td><td>93.3%</td></tr>
<tr><td>FPS + GRU</td><td>30.0%</td><td>70.0%</td></tr>
<tr><td><b>Ours</b></td><td><b>76.7%</b></td><td><b>56.7%</b></td></tr>
</table>

<center>图 5：在多样场景中评估机器人避障性能，包括复杂地形和动态人为干扰。</center>
<center>图 6：仿真消融实验场景设置。</center>

### 4.4 真实世界部署

我们评估了在各种场景下的全向避障有效性及其反应式行为，如图 1 所示。此外，我们在几种极端条件下测试了其性能。如图 5 所示，机器人在茂密草丛中成功穿越岩石地形，甚至同时受到移动人员的干扰。它还展示了对快速接近的空中障碍物和频繁人为阻碍的适应性。表 4 展示了在不同障碍物类型下，与 Unitree 原生系统在 30 次试验中的成功率定量比较。

**表 4：Omni-Perception 与 Unitree 原生系统的真实世界性能比较。基于每种涉及不同类型障碍物的场景 30 次试验报告成功率。粗体值表示该场景中更高的成功率。**

<table>
<tr><th>场景</th><th>Omni-Perception</th><th>Unitree System</th></tr>
<tr><td>静态障碍物</td><td>30/30 (100%)</td><td>30/30 (100%)</td></tr>
<tr><td>空中障碍物</td><td><b>21/30 (70%)</b></td><td>0/30 (0%)</td></tr>
<tr><td>小型障碍物</td><td>25/30 (83%)</td><td><b>30/30 (100%)</b></td></tr>
<tr><td>移动人员</td><td><b>27/30 (90%)</b></td><td>0/30 (0%)</td></tr>
</table>

---

## 5 结论

总之，我们提出了 Omni-Perception，一个端到端的强化学习框架，通过直接利用原始 LiDAR 点云数据，成功为足式机器人实现了鲁棒的全向碰撞避免。通过 PD-RiskNet 架构和在高保真 LiDAR 仿真环境中的训练，我们的方法集成了感知与控制，使机器人能够在复杂环境中移动，避开多样化的静态和动态障碍物，同时维持期望的速度指令。真实世界实验验证了该系统在具有挑战性的动态 3D 环境中执行敏捷和安全运动的能力。

---

## 6 局限性

**环境几何：** 在以高度非结构化的植被（例如茂密草丛）为主的环境中，性能可能会下降，因为 LiDAR 难以提取对运动至关重要的可靠几何特征。这构成了与在视觉复杂场景中提到的传感器局限性类似的挑战。在框架中引入语义分割是一个潜在的解决方案。

**Sim2Real 保真度权衡：** 当前方法在高保真模拟器中利用采样策略以促进 Sim-to-Real 迁移。虽然有效，但这涉及一个固有的权衡，与原始传感器输出相比，可能降低了机器人感知到的精细几何细节水平。在要求围绕复杂小型障碍物进行异常精确导航的场景中，这种简化可能成为限制因素，可能导致次优路径或碰撞。进一步的研究可以侧重于提高仿真保真度，开发更先进的域随机化或自适应技术，或结合在线学习以在部署时微调感知。

### 6.1 失败案例

**极度非结构化环境：** 尽管我们在此环境中取得了成功的示例，但机器人运动策略的有效性将大大降低。机器人将图片左侧的草识别为危险障碍物，并且由于距离较近，它迅速向左移动。

<center>图 7：茂密草丛。</center>

由于整个通道狭窄，机器人被迫进入图片右侧的草丛。进入草丛后，机器人周围环境被感知为危险区域，导致任务失败。

**过小且稀疏的物体：**

由于我们对远端点云进行平均，非常小的物体的特征将被破坏，使得无法对此类障碍物做出正确响应。

<center>图 8：细杆。</center>

---

## 参考文献

---

## 附录

### A 实现细节

#### A.1 奖励

**表 5：Omni-Perception 的奖励结构**

<table>
<tr><th>奖励项</th><th>公式</th><th>权重</th></tr>
<tr><td colspan="3"><b>Omni-Perception 奖励</b></td></tr>
<tr><td>带避障的速度跟踪 (r<sub>vel, avoid</sub>)</td><td>exp{−β<sub>va</sub>||v<sub>t</sub> − (v<sub>t</sub><sup>cmd</sup> + V<sub>t</sub><sup>avoid</sup>)||<sup>2</sup>}</td><td>2</td></tr>
<tr><td>距离最大化 (r<sub>ray</sub>)</td><td>∑<sub>i=1</sub><sup>n</sup> min(d<sub>t,i</sub>, d<sub>max</sub>) / (n·d<sub>max</sub>)</td><td>1.5</td></tr>
<tr><td colspan="3"><b>辅助奖励</b></td></tr>
<tr><td>z 轴线速度</td><td>v<sub>z</sub><sup>2</sup></td><td>−3 × 10<sup>−4</sup></td></tr>
<tr><td>足部磕绊</td><td>||Force<sub>xy</sub>||<sup>2</sup></td><td>−2 × 10<sup>−2</sup></td></tr>
<tr><td>连杆碰撞</td><td>||Force<sub>xy</sub>||<sup>2</sup></td><td>−0.02</td></tr>
<tr><td>关节限位违反</td><td>1<sub>q<sub>t</sub>&gt;q<sub>max</sub>||q<sub>t</sub>&lt;q<sub>min</sub></sub></td><td>−0.2</td></tr>
<tr><td>关节力矩</td><td>||τ||<sup>2</sup></td><td>−1 × 10<sup>−6</sup></td></tr>
<tr><td>关节速度</td><td>||\dot{q}||<sup>2</sup></td><td>−1 × 10<sup>−6</sup></td></tr>
<tr><td>关节加速度</td><td>||\ddot{q}||<sup>2</sup></td><td>−2.5 × 10<sup>−7</sup></td></tr>
<tr><td>动作平滑</td><td>||a<sub>t</sub> − a<sub>t-1</sub>||<sup>2</sup></td><td>−5 × 10<sup>−3</sup></td></tr>
<tr><td>动作平滑率</td><td>||a<sub>t</sub> − 2a<sub>t-1</sub> + a<sub>t-2</sub>||<sup>2</sup></td><td>−5 × 10<sup>−3</sup></td></tr>
</table>

### B 网络架构细节

Omni-Perception 框架为感知（PD-RiskNet）和控制（Actor）使用了特定的神经网络架构。

#### B.1 PD-RiskNet 架构

PD-RiskNet 处理时空 LiDAR 点云数据。如正文第 3.3.1 节所述，原始点云被划分为近端 \(P_{\mathrm{proximal}}\) 和远端 \(P_{\mathrm{distal}}\) 子集。

**输入处理：** 近端和远端路径都处理点云数据的历史。论文提到使用长度为 \(N_{\mathrm{hist}}\) 的历史（第 3.1 节，第 3.3.1 节）。根据提供的额外细节，我们使用 \(N_{\mathrm{hist}} = 10\)，意味着每个门控循环单元处理源自连续 10 次 LiDAR 扫描的特征。  
**近端路径：** 近端点云 \(P_{\mathrm{proximal}}\) 在馈送至专用 GRU 之前经过最远点采样。该 GRU 在训练期间使用特权高度信息进行监督。近端 GRU 的输出嵌入维度为 187 个特征。  
**远端路径：** 远端点云 \(P_{\mathrm{distal}}\) 使用平均下采样处理。来自当前和前 \(N_{\mathrm{hist}} - 1\) 帧（形成 10 帧序列）的特征被馈送至一个单独的 GRU。远端 GRU 的输出嵌入维度为 64 个特征。

来自近端和远端 GRU 的嵌入与本体感觉历史和指令向量串联后，再传递给 Actor 网络。

#### B.2 Actor 网络架构

运动策略（Actor）实现为一个多层感知机。

**输入：** 来自 PD-RiskNet 的串联特征（187 + 64 个特征）、处理后的本体感觉历史以及当前速度指令。  
**隐藏层：** MLP 由具有以下隐藏维度的顺序全连接层组成：[1024, 512, 256, 128]。通常在层之间使用适当的激活函数（ELU）。  
**输出层：** 最后一层输出机器人执行器的目标关节位置。输出维度为 12。

### C PPO 超参数

策略使用近端策略优化算法 [63] 进行训练。训练期间使用的关键超参数列于表 6。

**表 6：PPO 超参数**

<table>
<tr><th>参数</th><th>值</th></tr>
<tr><td>PPO 裁剪参数 (ε)</td><td>0.2</td></tr>
<tr><td>GAE λ</td><td>0.95</td></tr>
<tr><td>奖励折扣因子 (γ)</td><td>0.99</td></tr>
<tr><td>学习率</td><td>1 × 10<sup>−3</sup></td></tr>
<tr><td>学习率调度</td><td>自适应</td></tr>
<tr><td>价值损失系数</td><td>1.0</td></tr>
<tr><td>使用裁剪价值损失</td><td>True</td></tr>
<tr><td>熵系数</td><td>0.01</td></tr>
<tr><td>期望 KL 散度</td><td>0.01</td></tr>
<tr><td>最大梯度范数</td><td>1.0</td></tr>
<tr><td>环境数量</td><td>4096</td></tr>
<tr><td>每批环境步数</td><td>24</td></tr>
<tr><td>每批学习轮数</td><td>5</td></tr>
<tr><td>每轮小批量数</td><td>4</td></tr>
</table>

### D 域随机化细节

为改善 Sim-to-Real 迁移，我们在训练期间对多种仿真参数应用了域随机化。我们遵循先前工作 [7] 对机器人物理属性进行随机化。对于 LiDAR 感知，我们引入了特定的随机化：对 \(10\%\) 的点云应用随机掩码，赋予这些点从 [0, 0.3] 均匀采样的小距离值。此外，向测量的 LiDAR 距离随机添加了 \(10\%\) 的噪声。

具体参数及其随机化范围详见表 7。在强化学习期间，所有列出的属性均在全部 4096 个并行环境中均匀采样。

**表 7：域随机化的参数与范围**

<table>
<tr><th>参数</th><th>范围</th></tr>
<tr><td>LiDAR 点掩码比率</td><td>10% (值 ∈ [0, 0.3])</td></tr>
<tr><td>LiDAR 距离噪声比率</td><td>10%</td></tr>
<tr><td>附加质量 (kg)</td><td>-1.0 到 5.0</td></tr>
<tr><td>负载质量 (kg)</td><td>-1.0 到 3.0</td></tr>
<tr><td>质心 x (m)</td><td>-0.1 到 0.1</td></tr>
<tr><td>质心 y (m)</td><td>-0.15 到 0.15</td></tr>
<tr><td>质心 z (m)</td><td>-0.2 到 0.2</td></tr>
<tr><td>地面摩擦系数</td><td>0.40 到 1.00</td></tr>
<tr><td>地面恢复系数</td><td>0.00 到 1.00</td></tr>
<tr><td>电机强度（缩放因子）</td><td>0.8 到 1.2</td></tr>
<tr><td>关节校准偏移 (rad)</td><td>-0.02 到 0.02</td></tr>
<tr><td>重力偏移 (m/s²)</td><td>-1.0 到 1.0</td></tr>
<tr><td>本体感觉延迟 (s)</td><td>0.005 到 0.045</td></tr>
</table>

### E Taichi LiDAR 效率

我们在三台不同的计算机上测试了 Taichi 版本的性能，包括一台 MacBook。我们的程序像 MuJoCo、Genesis、Gazebo、Isaac Sim/Gym 一样是跨平台的。

在几何体较少（低于 200 个）的场景中，使用 115,200 条射线进行仿真可以实现 \(500\mathrm{Hz} +\) 的仿真效率，这非常快！大部分时间花费在准备过程中，占比很大（超过 \(60\%\)）。
